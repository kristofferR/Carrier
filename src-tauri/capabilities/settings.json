{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "The trusted local settings window. It calls Carrier's own commands (get/set settings, check for and explicitly install updates) and reads the app version.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
