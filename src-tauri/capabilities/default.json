{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal release capabilities for Carrier's Messenger windows. Every listed remote origin's own scripts—not just Carrier's injection—can call these commands, so grant only event emission, unread badge, notification permission query, and the scoped external opener that the page actually uses. meta.com/oculus.com are deliberately absent because they are transient login redirects where injected features can fail soft.",
  "windows": ["main", "win-*"],
  "remote": {
    "urls": [
      "https://facebook.com",
      "https://*.facebook.com",
      "https://messenger.com",
      "https://*.messenger.com",
      "https://fbcdn.net",
      "https://*.fbcdn.net",
      "https://fbsbx.com",
      "https://*.fbsbx.com"
    ]
  },
  "permissions": [
    "core:event:allow-emit",
    "core:window:allow-set-badge-count",
    "notification:allow-is-permission-granted",
    "notification:allow-request-permission",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "http://*" },
        { "url": "https://*" },
        { "url": "mailto:*" },
        { "url": "tel:*" },
        { "url": "x-apple.systempreferences:com.apple.preference.security*" }
      ]
    }
  ]
}
